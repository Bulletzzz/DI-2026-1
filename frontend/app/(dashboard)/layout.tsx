import CascaDashboard from "@/app/components/organismos/CascaDashboard"

export default function LayoutDashboard({ children }: { children: React.ReactNode }) {
  return <CascaDashboard>{children}</CascaDashboard>
}
